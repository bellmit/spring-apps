eureka:
  client:
    serviceUrl:
      #注册中心的地址
      defaultZone: http://192.168.1.150:8761/eureka/

server:
  #当前服务端口号
  port: 8080
  max-http-header-size: 10000000

spring:
  application:
    #当前应用名称
    name: datag-dev
